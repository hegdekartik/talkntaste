import 'dotenv/config';
import { checkBatchJob } from './sarvam.js';

async function run() {
  // If the user has a pending job, they can paste it here, 
  // but let's just make a new one and poll it.
  console.log("We need a job ID to check.");
}
run();
